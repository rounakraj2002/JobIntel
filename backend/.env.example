# Backend environment example
# Copy to backend/.env locally and fill values. Do NOT commit secrets.

# App
PORT=3000

# Database
MONGO_URI=your-mongodb-uri

# JWT
JWT_SECRET=your_jwt_secret

# GitHub OAuth
GITHUB_CLIENT_ID=your_github_client_id
GITHUB_CLIENT_SECRET=your_github_client_secret
# When deployed to Render, set this to: https://<your-backend>.onrender.com/auth/github/callback
GITHUB_CALLBACK_URL=https://<your-backend>.onrender.com/auth/github/callback
